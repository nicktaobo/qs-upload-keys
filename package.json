{
  "name": "qushu-upload-keys",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "start": "node server/index.js",
    "server": "node --env-file=.env server/index.js",
    "server:dev": "node --watch --env-file=.env server/index.js",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  },
  "dependencies": {
    "pg": "^8.16.3",
    "undici": "^7.29.0"
  }
}
